services:
  api:
    build: .
    ports:
      - "5000:8080"
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=true
      - PORT=8080
    volumes:
      - ./outputs:/app/outputs
      - ./.cache:/app/.cache
    restart: unless-stopped
